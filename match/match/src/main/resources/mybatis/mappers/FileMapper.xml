<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.club.match.Mapper.FileMapper">

    <select id="selectAt" resultType="AttachmentFileDTO">
        SELECT * FROM postattachment_tbl WHERE postAttachmentId=#{postAttachmentId};
    </select>

    <insert id="uploadFile" useGeneratedKeys="true" keyProperty="postAttachmentId">
        INSERT INTO postattachment_tbl (postId, attachmentUrl, originalFileName, contentType)
        VALUES (#{postId}, #{attachmentUrl}, #{originalFileName}, #{contentType});
    </insert>

    <delete id="deleteFile">
        DELETE FROM postattachment_tbl WHERE postAttachmentId = #{postAttachmentId};
    </delete>
    <select id="chatFileDownload">
        SELECT originalFileName FROM chatattachment_tbl where attachmentUrl like #{fileName};
    </select>

    <select id="selectFilesByPostId" resultType="AttachmentFileDTO">
        SELECT * FROM postattachment_tbl WHERE postId = #{postId};
    </select>

    <update id="updatePostIdAndUrl">
        UPDATE postattachment_tbl
        SET postId = #{postId}, attachmentUrl = #{attachmentUrl}
        WHERE postAttachmentId = #{postAttachmentId};
    </update>

    <select id="selectTempFilesByPostId" resultType="AttachmentFileDTO">
        SELECT * FROM postattachment_tbl WHERE postId = #{postId};
    </select>

    <update id="updatePostIdAndUrl">
        UPDATE postattachment_tbl
        SET postId = #{postId}, attachmentUrl = #{attachmentUrl}
        WHERE postAttachmentId = #{postAttachmentId};
    </update>

</mapper>