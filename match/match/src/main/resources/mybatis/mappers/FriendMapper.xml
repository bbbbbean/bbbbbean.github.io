<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.club.match.Mapper.FriendMapper">

    <!-- 유저 ID로 유저 정보 조회 -->
    <select id="findUserById" resultType="com.club.match.Domain.DTO.UserDTO">
        SELECT * FROM user_tbl
        WHERE userId = #{userId}
    </select>
    <select id="findUserByNickName">
        SELECT * FROM user_tbl WHERE nickName LIKE CONCAT('%', #{nickName}, '%');
    </select>

    <!-- 친구 요청 존재 여부 확인 -->
    <select id="existsFriendRequest" resultType="int">
        SELECT COUNT(*)
        FROM friend_tbl
        WHERE userId = #{userId}
        AND friendId = #{friendId}
    </select>

    <!-- 친구 요청 추가 (status = 2: 요청 대기 상태) -->
    <insert id="insertFriendRequest">
        INSERT INTO friend_tbl (userId, friendId, status)
        VALUES (#{userId}, #{friendId}, 3)
    </insert>

    <!-- 받은 사람 기준으로 친구 요청 목록 조회 -->
    <select id="findRequestsByFriendId" resultType="com.club.match.Domain.DTO.FriendDTO">
        SELECT * FROM friend_tbl
        WHERE friendId = #{friendId}
        AND status = 3
    </select>

    <!-- 친구 요청 거절 (요청 자체 삭제) -->
    <delete id="deleteFriendRequest">
        DELETE FROM friend_tbl
        WHERE userId = #{userId}
        AND friendId = #{friendId}
        AND status = 3
    </delete>

    <!-- 친구 삭제 (양방향 삭제 포함) -->
    <delete id="deleteFriend">
        DELETE FROM friend_tbl
        WHERE (userId = #{userId} AND friendId = #{friendId})
        OR (userId = #{friendId} AND friendId = #{userId})
    </delete>

    <!-- 친구 상태 변경 (예: 수락: 0, 즐겨찾기: 1, 차단: 2) -->
    <update id="updateFriendStatus">
        UPDATE friend_tbl
        SET status = #{status}
        WHERE userId = #{userId}
        AND friendId = #{friendId}
    </update>

    <!-- 친구 목록 조회 (status = 0: 일반 친구) -->
    <select id="getFriendList" resultType="com.club.match.Domain.DTO.FriendDTO">
        SELECT *
        FROM friend_tbl
        WHERE userId = #{userId}
        AND status = #{status}
    </select>

</mapper>
